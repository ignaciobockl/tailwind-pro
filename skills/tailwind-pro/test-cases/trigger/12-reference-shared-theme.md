# Trigger: @reference for shared theme

User wants to use `@reference` to share a common theme file (tokens) across multiple CSS entry points without duplicating @theme.

Context: Reusing theme tokens between separate CSS bundles.

Should activate tailwind-pro for @reference theme sharing.