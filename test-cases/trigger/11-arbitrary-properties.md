# Trigger: Arbitrary properties

User wants to apply arbitrary CSS properties using bracket syntax like `[mask-type:luminance]` and `[color-scheme:dark]`.

Context: Setting non-standard utility properties inline.

Should activate tailwind-pro for arbitrary properties.