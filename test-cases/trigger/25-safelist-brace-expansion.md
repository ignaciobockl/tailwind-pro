# Trigger: Safelist brace expansion

User wants to safelist the full color palette using brace expansion (e.g. `bg-{red,blue,green}-{50,100,...,900}`).

Context: Preserving dynamically-composed class names from purge.

Should activate tailwind-pro for safelist brace expansion.