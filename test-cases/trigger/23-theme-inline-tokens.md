# Trigger: @theme inline semantic tokens

User wants `@theme inline` for semantic tokens that reference other tokens (e.g. `--color-surface: var(--color-background)`) resolved at build time.

Context: Layering semantic tokens over primitive color scales.

Should activate tailwind-pro for @theme inline token patterns.