# Non-trigger: TypeScript generic types

User wants to define generic TypeScript types and interfaces for a data model.

Context: Pure TypeScript typing with no CSS involved.

Should NOT activate tailwind-pro.