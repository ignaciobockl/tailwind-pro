# Non-trigger: React Server Components

User wants to build React Server Components with async data fetching and streaming, without any styling concerns.

Context: Server-side rendering architecture.

Should NOT activate tailwind-pro.