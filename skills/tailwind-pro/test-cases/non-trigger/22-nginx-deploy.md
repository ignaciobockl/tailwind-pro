# Non-trigger: Nginx server config

User wants to write Nginx configuration for reverse proxying and static file serving.

Context: Server deployment and infrastructure.

Should NOT activate tailwind-pro.