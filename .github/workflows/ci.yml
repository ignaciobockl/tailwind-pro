name: CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint markdown files
        run: |
          npx --yes markdownlint-cli2 "skills/tailwind-pro/rules/**/*.mdc" "README.md" "CONTRIBUTING.md" "CODE_OF_CONDUCT.md" "CHANGELOG.md" "SECURITY.md" "skills/tailwind-pro/SKILL.md" "skills/tailwind-pro/test-cases/**/*.md" "skills/tailwind-pro/eval/*.md" || true
      - name: Check SKILL.md frontmatter
        run: |
          if ! grep -q '^name:' skills/tailwind-pro/SKILL.md; then echo "SKILL.md missing 'name' field"; exit 1; fi
          if ! grep -q '^description:' skills/tailwind-pro/SKILL.md; then echo "SKILL.md missing 'description' field"; exit 1; fi

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate YAML
        run: |
          npx --yes js-yaml skills/tailwind-pro/eval/quantitative-metrics.yaml > /dev/null && echo "YAML valid" || { echo "YAML invalid"; exit 1; }
      - name: Check test cases structure
        run: |
          test -d skills/tailwind-pro/test-cases/trigger && echo "trigger dir OK"
          test -d skills/tailwind-pro/test-cases/non-trigger && echo "non-trigger dir OK"
          test -d skills/tailwind-pro/rules && echo "rules dir OK"
          test -d skills/tailwind-pro/snippets && echo "snippets dir OK"
