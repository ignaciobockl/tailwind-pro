# Trigger: Multiple custom variants

User needs to define several custom variants with `@custom-variant` (e.g. `print`, `hocus`, `theme-dark`) to compose new selector variants.

Context: Extending the variant system with multiple project-specific variants.

Should activate tailwind-pro for custom variants.