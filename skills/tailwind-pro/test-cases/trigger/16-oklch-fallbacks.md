# Trigger: OKLCH browser support and fallbacks

User wants OKLCH color tokens plus fallback values for older browsers that lack OKLCH support.

Context: Ensuring colors degrade gracefully in legacy browsers.

Should activate tailwind-pro for OKLCH fallback patterns.