# Trigger: @theme static variables

User wants to force generation of static CSS variables using `@theme static` so tokens are emitted as real CSS custom properties with namespaced names.

Context: Exposing theme tokens as stable CSS variables for external consumers.

Should activate tailwind-pro for @theme static patterns.