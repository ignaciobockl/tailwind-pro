# Non-trigger: Remix Vite server build

User wants to configure a Remix app's Vite server build and deployment pipeline.

Context: Build tooling and deployment configuration.

Should NOT activate tailwind-pro.