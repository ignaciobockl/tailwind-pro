# Trigger: CSS Modules with @reference

User wants to combine CSS Modules with Tailwind by using `@reference` to import Tailwind utilities into a module.css file and migrate utilities into scoped modules.

Context: Migrating utility classes into CSS Module files while keeping Tailwind theme.

Should activate tailwind-pro for CSS Modules @reference.