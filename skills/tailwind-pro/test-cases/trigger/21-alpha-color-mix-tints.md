# Trigger: alpha and color-mix dynamic tints

User wants dynamic tint/shade generation using the `--alpha()` modifier and `color-mix()` function on theme colors.

Context: Creating lighter/darker shades without hardcoding each step.

Should activate tailwind-pro for alpha and color-mix tinting.