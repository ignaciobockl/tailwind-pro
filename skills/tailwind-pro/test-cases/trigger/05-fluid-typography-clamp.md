# Trigger: Fluid typography with clamp

User needs responsive text sizes using clamp() and CSS variables.

Context: Headings should scale smoothly between mobile and desktop.

Should activate tailwind-pro for fluid typography.
