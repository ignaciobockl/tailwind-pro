# Trigger: Named container queries

User wants named container queries using `@container/main` and responsive variants like `@sm/main:` for scoped breakpoint styling.

Context: Multiple container contexts with named breakpoints.

Should activate tailwind-pro for named container queries.