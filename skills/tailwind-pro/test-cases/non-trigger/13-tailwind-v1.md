# Non-trigger: Tailwind v1 legacy

User is working with Tailwind CSS v1 and its pre-JIT configuration using PostCSS plugins directly.

Context: Legacy Tailwind v1 setup without JIT engine.

Should NOT activate tailwind-pro.