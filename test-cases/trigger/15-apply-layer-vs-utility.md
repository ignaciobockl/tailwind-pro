# Trigger: @apply in component layer vs @utility

User is deciding whether to use `@apply` inside `@layer components` or define a custom `@utility` for a reusable style, and needs to know which to use when.

Context: Choosing the right abstraction for shared component styles.

Should activate tailwind-pro for @apply vs @utility guidance.