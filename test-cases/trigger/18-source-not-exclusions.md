# Trigger: @source not exclusions

User wants to exclude specific files or directories from content scanning using `@source not`.

Context: Preventing auto-detection from pulling in unwanted class sources.

Should activate tailwind-pro for @source exclusion patterns.