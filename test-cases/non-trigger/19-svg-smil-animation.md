# Non-trigger: SVG animation with SMIL

User wants to animate SVG graphics using SMIL elements like `<animate>` and `<animateTransform>`.

Context: SVG presentation animation.

Should NOT activate tailwind-pro.