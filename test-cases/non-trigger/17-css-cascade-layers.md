# Non-trigger: CSS @import cascade layers

User wants to structure native CSS cascade layers using `@import ... layer(...)` and `@layer` without any Tailwind.

Context: Plain CSS cascade layering architecture.

Should NOT activate tailwind-pro.