# Trigger: Dark mode with data-theme

User wants a dark mode strategy driven by a `data-theme` attribute instead of the `.dark` class selector.

Context: Theming via data attributes across the app.

Should activate tailwind-pro for data-theme dark mode strategies.