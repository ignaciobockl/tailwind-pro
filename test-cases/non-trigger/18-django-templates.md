# Non-trigger: Django templates styling

User wants to style Django templates using the template engine and inline HTML attributes.

Context: Python/Django server-rendered templates.

Should NOT activate tailwind-pro.