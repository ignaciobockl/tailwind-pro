# Trigger: Fluid spacing with clamp

User wants fluid spacing scale using `--spacing` with `clamp()` in addition to fluid typography.

Context: Responsive padding/gap that scales with viewport.

Should activate tailwind-pro for fluid spacing patterns.