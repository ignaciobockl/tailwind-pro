# Trigger: Reduced motion with custom keyframes

User wants custom `@keyframes` defined in `@theme` plus a `motion-reduce` variant to disable animation for prefers-reduced-motion users.

Context: Accessible custom animations that respect user motion preferences.

Should activate tailwind-pro for reduced motion and keyframes.